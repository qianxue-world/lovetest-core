apiVersion: v1
kind: Service
metadata:
  name: lovetest-api
  labels:
    app: lovetest-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: lovetest-api
